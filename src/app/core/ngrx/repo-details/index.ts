import * as repoDetailsActions from './repo-details.actions';
import * as repoDetailsSelectors from './repo-details.selectors';

import {
  repoDetailsFeatureKey,
  State as repoDetailsState,
  reducer as repoDetailsReducer,
} from './repo-details.reducer';

export {
  repoDetailsActions,
  repoDetailsSelectors,
  repoDetailsFeatureKey,
  repoDetailsState,
  repoDetailsReducer,
};
